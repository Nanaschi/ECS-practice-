using System.Collections;
using System.Collections.Generic;
using New_Folder._Scripts;
using Unity.Entities;
using Unity.Mathematics;
using Unity.Transforms;
using UnityEngine;

public partial class MoveSystem : SystemBase
{
    protected override void OnUpdate()
    {
        /*Entities.WithName("SphereUp").ForEach
        ((ref Translation position, ref SphereData SphereData) =>
        {
            position.Value.y += 0.1f;
        }).ScheduleParallel();
        
        
        Entities.WithName("largeCubeDataDown").ForEach
        ((ref Translation position, ref LargeCubeData largeCubeData) =>
        {
            position.Value.y -= 0.1f;
        }).ScheduleParallel();*/
    }
    
        
}
